# Tech Lead — Shared Memory Index (this repo)

This is the shared, cross-session memory for the **tech-lead** (Opus) and
**fabulous-tech-lead** (Fable) persona, **scoped to this repository**. Both
engines read and write here; this memory does not cross into other repos.

Each entry below points to one memory file in this directory. Keep this index to
one line per memory (`- [Title](file.md) — hook`); put the actual content in the
individual files, never here.

## Memories

<!-- Add entries as you learn durable technical facts. Example:
- [Non-UI e2e run via node test runner](non-ui-e2e-runner.md) — how integration tests are invoked
-->

- [Composition source of truth in repo](composition-source-of-truth-in-repo.md) — no Scene/Composition DB tables; supagloo.project.json manifest + S3 media
- [MinIO for local S3 parity](minio-local-s3-parity.md) — compose infra: MinIO dev bucket, dual S3 endpoints, one Postgres with app + DBOS system DBs
- [DBOS static workflows + enqueue pattern](dbos-static-workflows-and-enqueue-pattern.md) — hard constraint: static registration only; API enqueues via DBOSClient, workflowID = record id
- [GitHub App installation tokens](github-app-installation-tokens.md) — store only installationId; mint short-lived tokens on demand, no repo token at rest; the JIT zero-storage create-new-repo hop — task 62 proved it was BROKEN against real GitHub (no `auto_init` ⇒ unborn `main` ⇒ base PR 422s), **FIXED by plan row 63** (api sends `auto_init: true` + the workflow bootstraps an unborn base ref); no schema change was needed
- [OpenRouter media + AI SDK split](openrouter-media-and-ai-sdk-split.md) — OpenRouter covers video/TTS/music; generateObject for text, plain fetch for media; never hardcode model ids
- [KJV/BSB generation only](kjv-bsb-generation-only.md) — superseded 2026-07-18: any YouVersion-licensed translation now allowed; KJV/BSB is just the default
- [Prisma exact version pin](prisma-exact-version-pin.md) — consumers must pin database-lib's exact Prisma version, CI-enforced
- [prisma migrate dev is blocked by DBOS's table](prisma-migrate-dev-blocked-by-dbos-table.md) — `noop_proof` permanently drifts the dev DB; author migrations on a throwaway scratch DB + `migrate deploy`, never `migrate dev`/`reset`; Prisma 7.8 uses `--to-schema`/`--from-config-datasource`
- [vite cache poisons mutation testing](vite-cache-poisons-mutation-testing.md) — `rm -rf node_modules/.vite` between mutate→run→restore cycles; a stale transform cache faked a 29% failure rate at pristine HEAD
- [Gallery is NOT filterable by book](gallery-not-filterable-by-book.md) — sorts + `q` only; `book` removed from the query schema because book membership is YouVersion's authority, not a canon we hardcode
- [scriptureBook reference-shape rule](scripture-book-reference-shape-rule.md) — the deriver's three accept forms, the TWO rejected join rules (digit-gate, different-book → JHN), the closed article-alias set, and the deliberate 422 on chapter-free book ranges
- [Generated-frame sweep beats unit tests](generated-frame-sweep-beats-unit-tests.md) — for pure derivers, sweep `dist/` over reference/prose/all-pairs frames; it caught a mis-file that 147 tests and 8 killed mutations missed
- [E2E test-infra conventions](e2e-test-infra-conventions.md) — **real-provider gating e2e for ALL FOUR providers; there are NO stubs left** (§10 + §11, GitHub joined 2026-07-25); early /v1/test/seed, Stagehand real-stack mode with three nextjs lanes, crash/replay proven by system-DB step counts + real-host artifact reads (always `state:"all"`)
- [Compose infra + root test harness](compose-infra-and-root-test-harness.md) — task-3 built: postgres/minio/minio-init compose, POSTGRES_DB=postgres gotcha, reuse-or-spawn globalSetup, root Vitest split
- [Node API bootstrap](nodejs-api-bootstrap.md) — task-8 built: Fastify CJS on node:22-slim, zod type provider, zod env loader, /healthz, db-lib file:-dep + Docker symlink + openssl gotchas, migrate service, compose-override bridge; + Railway can't init submodules → Dockerfile git-clones db-lib at a pinned DATABASE_LIB_REF ARG (guardrail test enforces ARG↔submodule lockstep)
- [Provider-stub harness](provider-stub-harness.md) — **⛔ HISTORICAL: task-9's stub harness is FULLY DELETED (task 62).** `/__stub/*`, `/__admin/*`, github-stub, git-server and `tests/stubs/**` are all gone; do not build against it. Kept for the git-server CGI gotchas and tasks 9-38's history. Today's harness → real-github-e2e-harness
- [Real-GitHub e2e harness](real-github-e2e-harness.md) — task-62: the ONE prefix file + ONE network harness in root (dynamic-imported by api/dbos/nextjs), runtime installation discovery with five fail-fast throws, per-run private `auto_init` fixture repos, PAT-creates/installation-token-does-everything-else, **NO in-suite teardown ever** + the interactive archive-only cleanup script, and the `execFileSync`-leaks-the-token-via-argv trap
- [Auth & sessions built](auth-and-sessions-built.md) — task-10 built: opaque DB-backed sessions, SHA-256 token hash + sliding expiry, bearer plugin, /v1 prefix, invented YouVersion userinfo contract, flag-gated hard-404 seed; wire DTOs in db-lib (AuthUser not User)
- [In-flight db-lib e2e constraint](in-flight-dblib-e2e-constraint.md) — containerized API can't build against uncommitted db-lib (Dockerfile clones a pinned SHA) → e2e in-process (buildApp + postgres/stub subset, local prisma migrate); root/container e2e deferred to submodule bump
- [GitHub App connection built](github-app-connection-built.md) — task-11 built: signAppJwt+mintInstallationToken in db-lib (hand-rolled RS256), install-url/callback/disconnect/repos routes, GithubConnectionStatus (not GithubConnection) wire-name collision rule, required GITHUB_APP_ID/PRIVATE_KEY/SLUG; **the stub half is historical — task 62 repointed this at real api.github.com** (real Link-pagination proof, call counts reclassified to unit, 200-with-`error` exchange bug found)
- [S3 file presign service built](s3-file-presign-service-built.md) — task-13 built (OPENS M3): shared db-lib key helpers (buildAssetKey/parseS3Key), dual-endpoint client factory (sign against S3_PUBLIC_ENDPOINT, forcePathStyle), FilesService + GET /v1/files/presign-download with 404-on-any-denial ownership scoping; required S3_* env; e2e in-process vs Compose MinIO
- [OpenRouter + Gloo connections built](openrouter-gloo-connections-built.md) — task-12 built (CLOSES M2): POST openrouter (encrypt+keyLast4)/GET credits proxy/PUT gloo verify-then-store/DELETEs/merged GET connections; required SECRETS_ENCRYPTION_KEY (64-hex); gloo-stub `gloo-invalid` verify-failure sentinel; OpenRouterConnectionStatus/GlooConnectionStatus wire-name suffix rule
- [db-lib build must chmod +x bin](dblib-build-chmod-bin.md) — tsc strips exec bit on check-prisma-version.cli.js; rsync-into-consumer local flow then fails postinstall (exit 126); build script chmods it
- [Projects/versions read CRUD built](projects-versions-read-crud-built.md) — task-14 built: GET /v1/projects grid + GET/PATCH/DELETE /:id (rename=name-only, soft-delete=404-on-redelete) + /versions (real-semver desc via shared db-lib semver.ts); ProjectDto/ProjectVersionDto, ProjectNotFoundError 404-scoping; no create endpoint, no migration, no new env
- [Node DBOS bootstrap](nodejs-dbos-bootstrap.md) — task-15 built (OPENS DBOS): worker skeleton mirrors api minus migrate/port; DBOS SDK gotcha (registerWorkflow before launch, registerQueue AFTER); static registry.ts source-of-truth; two-DB env split (app vs supagloo_dbos system); self-managed noop_proof table; in-process DBOSClient idempotency e2e; root dbos compose service + bridge
- [Remotion template generator built](remotion-template-generator-built.md) — task-16 built: pure non-DBOS src/remotion/ generator (manifest→files), AbsoluteFill+named-Sequence composition, getAssetUrl/REMOTION_ASSET_BASE_URL seam, remotion 4.0.490 exact pin, real bundle() e2e via no-globalSetup config; applyManifest full-overwrite (manifest sole source of truth)
- [scaffoldProject workflow built](scaffold-project-workflow-built.md) — task-17 built: first real git-ops DBOS workflow (8 self-healing steps); ephemeral-workspace-vs-recovery gotcha solved by deterministic-commit rebuild; cancel+resume in-process crash test; ensureRepoAccessible via installation-repos list (absence is PERMANENT → the e2e must gate on installation visibility); new GITHUB_GIT_BASE_URL; stages schema kept local (promote to db-lib in task 18); Dockerfile runner needs git; **task 62 ran it against real GitHub and found `findOpenPrByHead`'s `state=open` kills a recoverable workflow after the base PR merges → `state=all`**
- [API job creation + polling built](api-job-creation-polling-built.md) — task-18 built: POST /v1/projects (create+scaffold enqueue) + GET job polling; API↔DBOS contract (workflow name/queue, stage catalogue, blank manifest, ScaffoldProjectPayload) promoted to db-lib; one-repo-one-project dedup + three distinct 409s + reusable git-ops guard; enqueue-only lazy DBOSClient + required DBOS_DATABASE_URL; markJobRunning step 0; in-process stand-in-worker e2e pattern
- [Manifest read built](manifest-read-built.md) — task-20 built: GET /v1/projects/:id/manifest?ref= SYNC Contents-API read (not a workflow); getRepositoryFileContents on makeGithubAppClient; 404/409/404/422 error split (422 = corrupt manifest); ManifestService 3-seam; ManifestRef/Response DTOs; **the `/__admin/contents` stub store is historical — task 62 replaced it with one real fixture repo + four real branches carrying real (and deliberately corrupt) files**
- [commitVersion workflow built](commit-version-workflow-built.md) — task-21 built: commitVersionWorkflow + POST /v1/projects/:id/commit; jobId-trailer idempotency + depth-2 shallow clone for a NON-deterministic real-message commit (3-case commitBranch, no double-commit on replay); updates working ProjectVersion IN PLACE (no bump/branch/PR); CommitManifestInvalid 422 / NoWorkingVersion 409; NOT baseHeadSha (deferred)
- [publishVersion workflow built](publish-version-workflow-built.md) — task-22 built (CLOSES 4 git-ops kinds): publishVersionWorkflow + POST /v1/projects/:id/publish {message}; 7 stages merge working→main + tag v<semver> + cut next branch; VERSIONING MODEL A (publish current working, next = nextPatchVersion of highest existing — NOT the 14a wireframe's literal numbers); no manifest (head-capture, no trailer); reuses scaffold openPullRequest/mergePullRequest + new createTag (422-idempotent); crash/replay parks at merge boundary → no duplicate PR
- [BFF foundation in Next.js built](bff-foundation-nextjs-built.md) — task-23 built the FIRST app/api/** handlers in supagloo-nextjs: httpOnly session cookie + generic forwardToApi bearer proxy + server-driven onboarding (retires localStorage) + extended ?seed= Stagehand seam (double-gated); db-lib NOT imported (pinned pre-auth-DTOs → hand-rolled contracts.ts); nonce for repeatable real-stack e2e; run pre-built API via node dist/server.js against compose PG
- [GitHub connect UI wired in Next.js](github-connect-ui-wired-nextjs.md) — task-24 wired the REAL GitHub App connect flow: BFF connect/callback/connections/repos routes (thin forwardToApi), poll-based effect layer (pending spans a real OAuth round-trip, window.open+poll GET /api/connections), connectGithub reducer w/ real @login+live repo count, mock-mode untouched; e2e "simulate GitHub redirect-back" via context.newPage→callback; live API needs a real RSA PEM + GITHUB_*_BASE_URL→github-stub
- [OpenRouter+Gloo connect UI wired in Next.js](openrouter-gloo-connect-ui-wired-nextjs.md) — task-25 (M4) wired REAL OpenRouter browser-PKCE (verifier/challenge→client callback page→exchange→key POST, popup+poll like github) + Gloo verify-then-store form (real inputs, live 400 surfaces as gloo-error); masked key sk-or-••••••{last4} + LIVE credits + real clientId on profile; connectProvider(provider,payload?); mock untouched; GOTCHA: Stagehand v3 = CDP understudy, NO context.route → fake OpenRouter's exchange with a window.fetch shim via context.addInitScript
- [Studio hydration + commit wired](studio-hydration-commit-wired-nextjs.md) — task-27 (M4) wired the studio off the DEMO_STORYBOARD mock: real GET :id+manifest hydration, a bidirectional manifest⇄storyboard MERGE adapter, real POST /commit + poll with a new COMMIT_FAILED state; server-shell mock/real branch (DEMO flag + session cookie); `project.manifest` presence is the mode signal; empty-manifest studio-empty guard
- [Publish wizard + version history wired](publish-wizard-version-history-wired-nextjs.md) — task-28 (M4) wired 14a Publish to real POST /publish + stage polling & 14b dropdown to GET /versions; real path ADDED alongside untouched mock two-step, keyed off project.manifest, Model-A one-step bump; new PUBLISH_REAL_* actions + publishOutcome + versionRowsFromDtos mapper; mock e2e 31 green, real spec deferred
- [AI generation API built](ai-generation-api-built.md) — task-31 built the 4 /v1/ai/generations routes: matrix-422 vs unwired-501 vs 400-structural gates, discriminated-union kind-specific input, API-authoritative cancel (conditional race-guard flip), enqueuer gained cancel(); shared db-lib matrix + wire DTOs; submodule-bump surfaced task-30 TranslationSchema blast radius
- [generateImage workflow built](generate-image-workflow-built.md) — task-32 built the FIRST media-generation workflow + FIRST real S3 write; callImageModel→fetch+upload-as-ONE-step (bytes never checkpointed) is the #33/#34 precedent; dbos internal-role S3 client + S3 env/config; wired image (un-501); MEDIA_RETRY choice
- [generateAudio workflow built](generate-audio-workflow-built.md) — task-33 (Step 6) built ONE generateAudio workflow (narration TTS + music, openrouter-only, dispatch-by-kind); reuses image's bytes-never-checkpoint fold (requestSpeech returns bytes → synth+upload ONE step); D2 music=same speech endpoint, D5 one combined narration asset, D6 X-Gen-Id→resultJson; db-lib green in-sibling, dbos/api green PENDING the Step-13 release+submodule-bump (in-flight window)
- [generateScript workflow built](generate-script-workflow-built.md) — task-30 built the FIRST ai-generation workflow (storyboard+script, schema-by-kind); repair loop = LLM step returns {ok} UNION (never throws on schema-fail) → every attempt checkpointed → exactly-once replay; creds loaded INSIDE the LLM step (never checkpointed); TranslationSchema broadened to free string (§9-Q10, rippled task-7/21 tests); YouVersion client to stub routes; programmable openrouter-stub chat-script
- [generateVideo workflow built](generate-video-workflow-built.md) — task-34 built the FIRST async-job/DBOS.sleep durable-poll/crash-replay media workflow + wired the LAST AI kind (video); submit persists providerJobId in-step (memoized→videoJobsCreated stays 1); pure pollUntilComplete loop (bounded 40×30s, env-overridable); removed MediaGenerationInputSchema; db-lib green, dbos/api RED-pending-bump
- [Failure-injection unit reclassification pattern](failure-injection-unit-reclassification-pattern.md) — task-34-E1 (§10.6): 503/repair/timing moved from e2e to injected-fetch UNIT tests (invoke call-fn twice over a sequenced fetch to model runStep retry; NO DBOS-in-process); where each reproduction lives; api needs none
- [Provider-call layer built](provider-call-layer-built.md) — task-29 (OPENS M5) built dbos src/providers/ step-helpers: AI-SDK generateObject wrapper (openrouter /api/v1 + gloo /ai/v2 .chat(), maxRetries:0 so DBOS owns retry), credential decrypt, mintGlooToken (scope=api/access), media client, discovery+TTL cache, no-model-ids lint; ProviderConfig singleton; +ai/@ai-sdk/openai deps; required SECRETS_ENCRYPTION_KEY; root stub fixes (gloo slash path + json_schema, openrouter output_modalities filter)
- [E2E secrets + Gloo naming-collision fix](e2e-secrets-gloo-naming-collision.md) — task-34-E2: GLOO_CONNECT_* for nextjs app-under-test creds (Stagehand's LLM keeps GLOO_CLIENT_*); harness-creds.ts resolvers + .env.example story across all 4 repos; §10.8 fail-fast splits to 34-E3/E4/E8
- [API e2e real-provider connection seeding](api-e2e-real-provider-connection-seeding.md) — task-34-E3: connections.e2e.ts seeds via its OWN real routes vs LIVE OpenRouter/Gloo; helper at src/testing/ (excluded from dist), fail-fast in helper not global-setup (34-E8), wrong-cred = mangled real secret, vitest doesn't auto-load .env
- [dbos e2e real-provider generate (34-E4)](task-34-e4-dbos-e2e-real-provider-generate.md) — de-stubbed 4 generate-*.e2e to real OpenRouter; src/testing seed-connections (direct-DB + mintGlooToken) + step-introspection (prefix-match, 34-E7 reuses) + e2e-models discovery; e2e is OpenRouter-only (no Gloo discovery → Gloo unit-proven), video crash/replay → it.todo(34-E7)
- [Flagship video crash/replay reworked (34-E7)](task-34-e7-flagship-crash-replay-video.md) — host-introspection-free proof (providerJobId stability helper + system-DB submitVideoJob count) via the in-process park/cancel/resume idiom; ZERO workflow change (task 34 already satisfied it); e2e lane isn't typechecked (enqueue<T> latent error); .env must be sourced to run
- [YouVersion real API + live e2e (34-E5)](task-34-e5-youversion-real-api.md) — verified-live Platform contract (collection `/v1/bibles?language_ranges[]=`, numeric id→string, passage PATH `/v1/bibles/{id}/passages/{USFM}`→`content`, 401/422/404); deterministic-failure via fetchPassage 401 (generate-script.ts untouched); USFM-upstream + non-numeric fallback = residual risk
- [YouVersion sign-in e2e rework (34-E6)](task-34-e6-youversion-signin-e2e-rework.md) — auth.e2e zero-egress seed mechanics (throwing verifier), FIRST loud-skip helper (resolveYouVersionLiveGate) + optional live spec; DISCOVERY: real sign-in is JWT-claims (POST /auth/token, yvp_id/sub/email/name/profile_picture, refresh tokens, ~1h) so shipped userinfo-GET verifier is known-wrong → task-55 follow-up; global-setup + stubs left for 34-E8
- [Harness simplification — 3 stubs deleted (34-E8)](task-34-e8-harness-simplification.md) — deleted openrouter/gloo/youversion stubs + all wiring (STUB_KIND image then 2 kinds; **task 62 later deleted those two and the whole directory**); global-setup fail-fast stays per-spec (NOT added to shared setup — over-coupling); Gloo HAS a live catalogue `/platform/v2/models` (new resolveGlooModel); providers.e2e reworked to real hosts + no-stub guard; 8 api specs' never-invoked YV verifier repointed to real host
- [E2E live provider-call surface](e2e-live-provider-call-surface.md) — reusable global-fetch setupFile logger + the confirmed live endpoint/model inventory of a full (paid) e2e run; root Compose e2e's api-container env gap
- [Studio AI wiring wired](studio-ai-wiring-nextjs.md) — task-35 (closes M5) wired reroll/rewrite/re-plan/narrator/music/first-gen → real POST /v1/ai/generations + poll + presign; BFF injects provider/model; previews from MinIO; commit persists refs; db-lib VoiceDescriptor+assetKey; mock inspector kept read-only to stay green; tsconfig excludes the nested submodule
- [Studio AI wiring follow-ups](studio-ai-wiring-followups.md) — task-57 closed 3 task-35 gaps: per-scene scripture carried through the UI Scene (kills re-plan reattachment bug) + post-commit project.manifest refresh, ai-config override/fallback logging + .env.example docs, in-flight preview generating overlay
- [Workspace + project wizards wired](workspace-project-wizards-wired-nextjs.md) — task-26 (M4) built the create-new-repo JIT user-auth hop (db-lib schemas + api user-auth client/service/2 routes + GITHUB_APP_CLIENT_ID/SECRET + nextjs popup+localStorage-handoff callback) + swapped the provisioning-log data source (fake ticker → polled ProjectJob.stages, view contract preserved) + real repo pickers/grid + §9-Q4 honest copy + disabled votd/passage/demo cards; isMock session field branches wizards; github-stub has NO reset route; real-stack Stagehand spec written but execution deferred per in-flight-dblib
- [renderWorkflow built](render-workflow-built.md) — task-36 built the Remotion render workflow: staticFile/public asset materialization (buckets are private), manifest re-materialization so synthesized audio is actually referenced, `<Audio>` in the generated composition, audio-as-steps (not a child workflow), env-configured fallback models, renderMedia never auto-retried
- [renderWorkflow gotchas](render-workflow-gotchas.md) — DBOSWorkflowCancelledError never sets `.name` (match `dbosErrorCode` 24/27); memoized steps + ephemeral workspace need a 4-level self-heal ladder; changing the Remotion template invalidates existing deterministic `v0.0.0` branches — **the stale-fixture half is RESOLVED BY DELETION (task 62: no git-server, per-run repos), but the determinism property is why per-run repo names are mandatory**
- [DBOS render child isolation](dbos-render-child-isolation.md) — allowlist child env (NEVER the git helper's `{...process.env}` shape), `--ignore-scripts` always, NDJSON stdin/stdout child protocol + tsx-CLI entry resolution, and the container-verified Remotion Chromium apt list
- [GitHub App PEM normalization](github-app-pem-normalization.md) — escaped-`\n` key normalized in db-lib `signAppJwt` (fixed 2026-07-25); api always normalized independently, DBOS never did → every git-ops workflow was dead; task 62 put the REAL PEM in every e2e (no more generated throwaway keypairs) and made api/dbos hand the PRODUCT signer to the harness — but THREE normalisation impls now exist, fenced by byte-identical-signature tests
- [Render API + UI built](render-api-and-ui-built.md) — tasks 37+38: 5 render endpoints + the 14c overlay off the fake ticker; framesTotal-0-is-indeterminate, mine=1 is a required literal, download delegates to FilesService, and the 14c stage order was REORDERED to match the worker (synth→bundle)
- [api /v1 scope has no global auth](api-v1-scope-has-no-global-auth.md) — bearerAuthPlugin only DECORATES (no instance hook), so a public route inside /v1 just omits preHandler; `optionalAuth` now SHIPPED (task 39) on that same plugin, resolve-if-present/never-401; + the TDD trap that `{preHandler: undefined}` makes an optionalAuth suite pass with NO implementation
- [Gallery backend built](gallery-backend-built.md) — tasks 39+40: the api's FIRST $queryRaw (one keyset builder, fixed ORDER BY map), FIRST optionalAuth route, FIRST ownership-free presign, the trending pagination epoch, and the never-raises upvote transaction
- [Bound is not safe — Postgres value gates](bound-is-not-safe-postgres-value-gates.md) — `Date.parse` is not a timestamptz parser, a NUL is not valid `text`, an unsafe int breaks the RESPONSE serializer; validate VALUES at the codec + the api's new `setErrorHandler` (ignores `error.status`, generifies every 500)
- [Tests that hold invariants vs shapes](tests-that-hold-invariants-vs-shapes.md) — three ways a green suite proved nothing: an un-tested "non-negotiable" rule, a mock-shape claim sold as behaviour, and an auth carve-out pinned only against a fake
- [Fastify auto-registers a HEAD twin](fastify-auto-head-twin.md) — it fires onRoute too, so a route-inventory test must filter HEAD rather than disabling `exposeHeadRoutes`
- [Vitest toContain is identity-based for objects](vitest-tocontain-object-identity.md) — a red test asserting `toContain(aDateTheTestBuilt)` against a reconstructed value is UNSATISFIABLE; use `expect.arrayContaining`, but keep identity for negative-about-an-injected-object checks
- [scriptureBook W6 — non-joiner cross-book](scripture-book-w6-nonjoiner-crossbook.md) — **CLOSED at db-lib `b13b846`** (2026-07-26): `"Genesis: Exodus 1:1"` -> EXO fixed by generalizing the joiner NOTION (any letter/digit-free run + 5 words), cross-book 642,339 -> 0 over 1.7M probes; all three candidates priced, and the unlisted-WORD residual is a pinned decision
- [scriptureBook's 11 inert guards](scripture-book-mutation-audit-inert-guards.md) — mutations that survive the suite AND provably change nothing (0 diffs / 253,462 frames); not coverage gaps, don't delete them either — W6's pass added a 12th (`joinsAnotherSegment`'s clause order, 0 diffs / 483,937 probes)
- [One rule, one module, many boundaries](one-rule-one-module-many-boundaries.md) — per-field validation ALWAYS leaves a field out; the shared `postgres-text` rule, its three differently-justified clauses, why the exempt set must be tested BEFORE `.trim()`, and the route-table test that covers routes not yet written
- [Gallery e2e seeding seam](gallery-e2e-seed-helper.md) — root's `gallery-e2e-seed.mjs`: why it HAS teardown (global listing) when the GitHub harness has none, the `e2e-gallery-` id gate, the pg-timestamp shift trap, real-upvote-rows rule, and why the MP4 must be genuinely playable
- [A test that claims a class must drive the class](a-test-that-claims-a-class-must-drive-the-class.md) — four vacuous-test shapes caught in one pass (hardcoded field, corpus filtered by the predicate under test, 400-demanded-where-200-is-correct, a global assumption about other specs' fixtures) + renders.e2e is noise, never quote its counts
- [Gallery UI built](gallery-ui-built.md) — row 41: the mount-gated grid needs a SECOND settle wait, an effect keyed on filter state makes a failed load permanent, the vote response has no `rank`, and `/gallery` exposed `nav-auth`'s `useYVAuth()` bug
- [nextjs eslint ignores vendored checkouts](nextjs-eslint-ignores-vendored-checkouts.md) — `npm run lint` was red at HEAD from the db-lib submodule; globalIgnores now matches tsconfig's exclude, so a lint failure is finally yours
- [The documented gate must be the gate that runs](documented-gate-must-be-the-gate-that-runs.md) — `DELETE … WHERE id LIKE` is NOT the reviewed predicate; select→filter→`ANY($1::text[])`, proved by a survivor row, and how to unit-test a 7-table deleter with no DB
- [nextjs unit lane can render components](nextjs-unit-lane-component-rendering.md) — jsdom opt-in via docblock + a hand-rolled 60-line renderer; `clearAllMocks` leaves the `…Once` queue (use `resetAllMocks`), unmount in `afterEach`
- [An isolation seam can hide the property](an-isolation-seam-can-hide-the-property.md) — `?q=<nonce>` fixture isolation meant every listing the gallery spec inspected was FILTERED, so four tests pinned "rank = position among search hits" while three JSDocs promised a global ordinal
- [A lane that builds its own inputs cannot prove the producer](a-lane-that-builds-its-own-inputs-cannot-prove-the-producer.md) — the render bundle e2e hand-wrote assets into `public/`, so 7 green proofs missed a render-KILLING missing download (Remotion throws on a 404, never degrades); plus the union invariant that replaced it and why its obvious phrasing is false
- [Gallery "making of" snapshot](gallery-making-of-snapshot.md) — Turn 16a's `GalleryItem.makingOf`: snapshot the manifest ONCE at publish (never read it on the public watch page), the `version: literal 1` reject-don't-half-read rule, the two deliberate absences, the knowingly-duplicated jsonb text gate — plus the api half: sanitize→truncate→validate (truncation can CREATE an unpaired surrogate), `Prisma.DbNull` vs `JsonNull`, `Promise.race` already handles the abandoned read, and the zero-egress/real-GitHub e2e split whose `E-MO2` pushes a real second commit
- [A spec that writes to a global surface owes teardown](a-spec-that-writes-to-a-global-surface-owes-teardown.md) — gallery.e2e leaked 47 public items per run (46 matching the nextjs guard's exact predicate); delete by tracked id, never by pattern, and prove it by running the spec twice
- [DBOS e2e per-lane schema isolation](dbos-e2e-lane-schema-isolation.md) — killed the "keep the Compose dbos container idle" precondition via `systemDatabaseSchemaName`; BOTH halves or it is decoration, assert BEFORE the first wait or the failure is a bare timeout, and the nine runtime-only dbos specs were a green-lie generator
- [Gallery watch-page pure seams](gallery-watch-page-pure-seams.md) — 16a slices C4/C5: the BFF GET that came back on its own stated condition, detail-vs-card parsing, two upvote formatters by surface, the stretched scene-poster ramp, and re-signing 15s before the 120s presign dies
- [Gallery watch page built](gallery-watch-page-built.md) — 16a slices C6/C7: nav `variant` vs a third `active`, the once-per-playback-attempt re-sign guard, why a vote response must not be spread over the detail item, and the four claims Stagehand structurally cannot make here
- [A new e2e spec joins the MOCK lane by default](new-e2e-spec-joins-the-mock-lane-by-default.md) — nextjs's mock lane globs everything and excludes real specs by name, so registering a real-stack spec is TWO edits
- [grep here is a shell function that eats `\|`](grep-shell-function-eats-bre-alternation.md) — BRE alternation silently matches nothing; use /usr/bin/grep and never trust a zero-hit as proof of absence
- [Publish-to-gallery dialog built](publish-to-gallery-dialog-built.md) — 16b slices C8/C9: the `key=` reset re-aimed at the SELECTED render, the client-side `<slug> · v<semver>` join that never hides a render, why a publish refusal must survive the fetch layer, and 4a's two departures from the drawing
- [A guard only reachable by an unusual path](a-guard-only-reachable-by-an-unusual-path.md) — a surviving mutation whose only distinguishing input was A→B-with-no-close; plus never ask a semantic `extract` to COUNT, and match an assertion's resolution to the fixture's
- [A publishing e2e frees its own render](a-publishing-e2e-frees-its-own-render.md) — un-publish a filler item to free its unique slot (no seed change), and why one leaked cuid row rolls the ENTIRE fixture teardown back
- [Owner-scoped 404 is ambiguous](owner-scoped-404-is-ambiguous.md) — "not yours" and "not there" are the same 404 by design (E-G15 pins it), so a cleanup loop that breaks on 404 silently leaks the row it meant to delete; only 200 means deleted
- [next dev compiles routes on first request](next-dev-compiles-routes-on-first-request.md) — a page warm doesn't compile its API route modules, so the first mutation pays a compile inside its own poll budget; warm them anonymously, and "hydration is not data" applies to /your-videos too
- [A guard satisfied by its own residue](a-guard-satisfied-by-its-own-residue.md) — lane-schema isolation left 84 `workflow*` tables behind, so the dbos-worker guard passed on a crash-looping container; `dbos.queues` ROWS is the discriminating predicate, proved with a rolled-back probe schema
- [Shared safety logic needs a marker fence](shared-safety-logic-needs-a-marker-fence.md) — two deliberate copies of `dbos-lane-isolation.ts` also duplicated the only gate on an interpolated `DROP SCHEMA`; BEGIN/END markers + a root byte-identity test that compares TEXT and refuses an empty fence
- [Session resolved ≠ signed out](session-resolved-vs-signed-out.md) — `isAuthed===false` also means "not asked yet"; act on `sessionResolved` and derive at render, never decide in the click handler
- [Modal panel is viewport-bounded](modal-panel-is-viewport-bounded.md) — a fixed backdrop is unscrollable, so cap the panel + scroll its body + `margin:auto`; prove it with hit-testing at phone widths
- [Render bugs: narration/music/Ken Burns built](render-bugs-narration-music-kenburns-built.md) — per-scene narration via the dedicated speech endpoint (ONE row, N assets in resultJson), music looped to the composition, deterministic index-derived Ken Burns; the 4-segment S3 key trap, the SIX nextjs length boundaries, and the bundle-lane proof spec
- [OpenRouter audio live contract facts](openrouter-audio-live-contract-facts.md) — four shipped "CONFIRMED" comments were WRONG: /api/v1/audio/speech exists, `speech` is a separate catalogue from `audio`, Lyria always returns MP3 whatever you ask for, and no music model takes a duration
- [Remotion CSS + Loop gotchas](remotion-css-and-loop-gotchas.md) — React mangles a numeric `scale` into `scale:1.1px` (silent no-op); a whole-video fade over looped audio needs loopVolumeCurveBehavior="extend"; OffthreadVideo refuses a still
- [Storyboard's six length functions mutation-pinned](storyboard-six-length-functions-mutation-pinned.md) — U-S14..19's stretched fixture kills all six raw-duration reverts; sceneEntryFrame's authored-duration clamp is deliberate, don't "fix" it
- [YouVersion Bible read surface lives in nextjs](youversion-bible-read-surface-lives-in-nextjs.md) — six BFF routes call api.youversion.com directly (not the api, not the SDK, zero db-lib); `page_size=*` returns the whole catalogue in ONE request, `fields[]` is ignored on /books (1.59 MB always), 204 = no Bibles, and the API's text_direction beats Intl
- [headCommitSha "ahead of main" is not an invariant](head-commit-sha-ahead-of-main-is-not-an-invariant.md) — sound after scaffold, only an OBSERVATION after publish (cutNextBranch takes no start-ref), undefined for imports; so the Publish gate is three-valued, matches the working row by BRANCH NAME, and fails open
- [Scaffold's merge-sha fix made a fresh publish impossible](scaffold-merge-sha-fix-made-a-fresh-publish-impossible.md) — dbos de745d2 put v0.0.1 at pr.mergeSha, so publishing a freshly-scaffolded project 422s "No commits between"; it silently reddened E-PUBR1 and studio-render-real, and an empty manifest has no script-input to commit through
- [RTL via dir="auto", not a manifest field](rtl-via-dir-auto-not-a-manifest-field.md) — dir="auto" is UAX#9 first-strong, a standardised algorithm every engine implements identically, so the preview (the user's browser) and the render (headless Chromium) agree without being the same engine; logical border properties, a `:dir(rtl)` CSS italic override instead of a script detector, and YouVersion's U+200E marks left intact
- [genesis-1 studio/share bugs built](genesis1-studio-share-bugs-built.md) — the 7 fixes (scripture picker, bounded 5..10 add/delete scene, SHARE popover prune, header Render button, Publish gate) with zero db-lib/api change; the new testids, and the two traps (a blank scriptText fails min(1) and makes the manifest uncommittable; only lib/config/env.ts may read YV_APP_KEY)
- [Gloo image generation — live facts](gloo-image-generation-live-facts.md) — Gloo DOES generate images via `POST /ai/v2/responses` (base64 inline), has NO speech/music/video (404 not 405), prices 106/106 models, and its `tradition` enum is NOT server-validated (a wrong value 200s and silently degrades)
- [genesis-1 Inspector: models, cost, video built](genesis1-inspector-model-cost-video-built.md) — project-level `aiSettings`, the first model-catalogue endpoint (api, because only it can decrypt Gloo creds), a three-state cost estimate that refuses to price video, per-scene video + the `visualAssetKind` producer, and the injected-matrix/forward-type trick that kept all five repos green through a db-lib change
- [The manifest has FIVE mirrors, not four](the-manifest-has-five-mirrors-not-four.md) — the api's manifest DTO validation is a `z.object` strip point that forwards its own parse output, so a four-mirror field is still erased on every commit AND read until the api's gitlink moves; no repo's tests can see it
- [`.passthrough()` ships a wire field before the db-lib bump](passthrough-ships-a-wire-field-before-the-dblib-bump.md) — a TOP-LEVEL key survives a pinned consumer (a nested one is stripped); plus `.extend()` at the api route so a new field is a loud 400 not a silent drop, and the measured, NON-recursive scope of the `no-model-ids` lint
- [`.passthrough()` exempts only UNDECLARED keys](passthrough-exempts-only-undeclared-keys.md) — the fact that decides whether a forward-declaration's runtime guard is dead or load-bearing at the db-lib bump; the index signature is `unknown` not `any`; and why an `as` in a FIXTURE makes the test unfalsifiable
- [A silent `return` is a green test that never ran](a-silent-return-is-a-green-test-that-never-ran.md) — E-SH2 skipped itself into three recorded 21/21 runs; plus why owner-scoping makes ANY cross-run project-slug fixture impossible, and the build-it-in-run path that works
- [The YouVersion SDK freezes the access token](youversion-sdk-freezes-the-access-token.md) — `useMemo(…, [])` over localStorage makes `accessToken` permanently null on the OAuth callback load; and why `serverUserId`, not `sessionResolved` or `isAuthed`, is the only safe gate for an owner-scoped fetch
- [A YouVersion verse RANGE is echoable, not constructed](youversion-verse-range-is-echoable.md) — `+`-joining echoed per-verse passageIds is a 200 the host normalises into a canonical `id` it hands back, while `PSA.121.1-PSA.121.4` is a 404 and `PSA.117.1-5` on a 2-verse chapter is a 200 with a FABRICATED reference; plus the 25-row default collection page
- [The wizard's passage must travel as USFM](wizard-passage-must-travel-as-usfm.md) — `ScripturePassageRequestSchema.reference` is a USFM id and the field NAME hid it, so the studio's per-scene rewrite had been failing every generation in production; the Genesis-1 "hallucination" was a MISSING input, not a hardcoded default
- [The ready card's redirect needs a server-confirmed slug](wizard-ready-card-redirect-needs-a-confirmed-slug.md) — automating the navigation turns plan-row-53's latent 404 into an unavoidable one, so the wizard asks `GET /api/projects/:id` instead of widening the create response; and every e2e acquisition helper had to stop clicking `open-in-studio` unconditionally
- [A baseline must hold every OTHER variable equal](a-baseline-must-hold-every-other-variable-equal.md) — a live "the walk returns more than one page" test survived `while (false)` because its baseline omitted the client's `page_size`, so it measured page_size while claiming to measure the walk
- [The DBOS worker had no YouVersion key](dbos-worker-had-no-youversion-key.md) — root docker-compose.yml passed YOUVERSION_APP_KEY to nextjs and nothing else, so every scripture read 401'd non-retryably and surfaced as "Generation failed — try again"; invisible because every e2e fixture was a blank project with no manifest.scripture to fetch
- [An e2e cascade `<select>` needs the OPTION, not the element](e2e-cascade-select-needs-the-option-not-the-element.md) — assigning a value a select does not offer is a silent no-op that fails one level downstream; plus why a select's DOM value is not a proxy for its React state, and why a passage preview is not a signal that the verse default settled
- [The studio Inspector requires scenes](studio-inspector-requires-scenes.md) — no scenes means no SceneInspector, so the scripture picker and the read-only project-passage line do not exist until after the first generation, and a spec reading them earlier gets "" from a missing element
- [`.optional()` does not weaken `.min(1)` for empty strings](optional-does-not-weaken-min1-for-empty-strings.md) — so a probe that BLANKS an env var cannot prove a required-ness change; only genuine absence discriminates, and that is the missing-Compose-wiring case rather than the operator-forgot one
- [A boot probe's `timedOut` needs ETIMEDOUT](e2e-boot-probe-timedout-needs-etimedout.md) — `docker compose` traps the harness's SIGTERM and exits POSITIVE, so `status === null` read `timedOut: false` on a 120 s hang; also `--rm` leaks the container on exactly that path, so name it and `rm -f` in a `finally`
- [Clear a derived selection on the effect's RE-RUN path](clear-derived-selection-on-effect-rerun.md) — a guard inside an effect's early return only fires when the input goes FALSY, so non-null→non-null transitions skip it and leave a stale value armed downstream
- [Voice catalogue must be provider-sourced](voice-catalogue-must-be-provider-sourced.md) — the curated table was wrong for EVERY model and an undocumented OpenAI→Kokoro alias hid it; `supported_voices` is live + unauthenticated, dies at 3 z.object strip points, and the dbos default voice is now DISCOVERED not constant
- [A change that moves no golden needs a behavioural pin](a-change-that-moves-no-golden-needs-a-behavioural-pin.md) — `muted` + the music duck moved zero golden bytes; the bundle-lane recipe (silent narration to measure one track, an `mp4a` precondition) and the three mutations that proved it
- [A failed read is not an answer about the user's data](a-failed-read-is-not-an-answer-about-the-user-s-data.md) — a never-throwing, never-retrying fetcher's `null` means "we could not ask"; branching on it as "the answer is no" wiped a committed narrator voice, and a write-when-defined serializer then wrote the loss into the repo — plus the picker-vs-action-button split that decides which direction the same `null` fails in
- [An anti-vacuity control belongs AFTER the loop](an-anti-vacuity-control-belongs-after-the-loop.md) — `typeof v === "string" || v === undefined` is unfalsifiable against a `string | undefined` field; a guard turns a wrong-reason pass into a vacuous one, and a fixture-blind loop can only be de-vacuumed by a post-loop `seen` control (prove it with mutation-plus-control-removed, in isolation)
- [Two entry points disagree about the opening scene](two-entry-points-disagree-about-the-opening-scene.md) — E-SH2's "silent data loss" was a spec reading the wrong scene: a generation selects `scenes[0]`, a fresh open selects `scenes[1]`, and `script-input` could not name its own subject until `data-scene-id`; settle this class against the fixture repo's git, not the serializer
- [Optional connections & provider-aware Studio (2026-07-31)](optional-connections-built.md) — the run that deleted the wizard's GitHub gate, put R3's guardrail at the launcher, folded R4/R6/R8 into ONE connection-aware resolver, and gave the api a second refusal class (409 `provider_not_connected`); plus the two calls the user did not specify
- [A permissive default opens a second doorway](a-permissive-default-opens-a-second-doorway.md) — gating only the `blocked` branch is always a half fix when the evaluator deliberately verdicts ALLOWED on an unresolved read; the unresolved window walked straight past R3's modal into the project wizard, and a composition test needs stubs that RENDER plus a not-gated control
- [Matrix order is compatibility, not preference](matrix-order-is-compatibility-not-preference.md) — `allowed[0]` disagrees with the real default for `script`, so clamp an unusable provider override to `DEFAULT_GENERATION_PROVIDERS[kind]`, ONCE before the repair (the `allowed.find` path is order-driven too); one env var was blanking the whole Studio AI surface
- [The api e2e seeds connections per spec](api-e2e-seeds-connections-per-spec.md) — real connect routes when the CIPHERTEXT is under test, direct Prisma rows when only EXISTENCE is; plus `tsconfig` never typechecks `tests/`, `ConnectionLookup` is structural so a stub hides the gate, and an id-derived 404 can pass for the wrong reason forever
